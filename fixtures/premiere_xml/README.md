These XML files are Premiere project XML samples, reference outputs, and scratch artifacts used for development.

- `reference_2026.xml` is the active test fixture used by the unit tests.
- The other XML files are not referenced by the code today, but they look like useful debugging/reference samples, so they were kept instead of deleted.
- `scratch-project.xml` is the former `scratch/project.xml` sample moved here to keep the repo root clean.
